---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: grafana-agent-operator
  namespace: default
spec:
  values:
    resources:
      limits:
        memory: 368Mi
      requests:
        memory: 368Mi
        cpu: 100m
